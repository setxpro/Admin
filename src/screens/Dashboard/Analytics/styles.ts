import styled from 'styled-components';

export const Container = styled.div`
    padding: 1rem;
    background: pink;
`;
export const ContentMessageSubscriberOrder = styled.div`
    background: pink;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
`;
export const ContentAvgSupport = styled.div`
    background: pink;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1.3rem;
`;

